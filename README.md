How to Run the Project:
Backend: Go to /backend, run npm install then npm run server.

Frontend: Go to /frontend, run npm install then npm start.

Admin: Go to /admin, run npm install then npm start.
